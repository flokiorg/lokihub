Release candidate for 0.5.0, covering everything merged to `main` since 0.5.0-rc.4.

## Fixes
- Fixed `cash_consolidate` delivering the merged wallet's token encrypted to the wrong key (`new_identity` instead of the calling connection), so a pubkey-mode consolidation's token couldn't be decrypted by its intended recipient.
