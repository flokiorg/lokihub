.DS_Store
.env
.env.local
*.db
*.macaroon
node_modules
nostr-wallet-connect
nwc.db
.breez
.data
.idea
.claude

frontend/dist
frontend/node_modules
frontend/wailsjs
package.json.md5

build/
ops/bin/

*.log
.agents
skills-lock.json

# generated by platform-specific files
frontend/src/utils/request.ts
frontend/src/utils/openLink.ts
frontend/.yarn
*.db-shm
*.db-wal
*.db-journal
data/
lokihub-services/
lokihub-store/
docs/audits/

# real NWC parent connection strings, provisioned per-machine
integration/config.local.yaml

# air's build output (docker-compose.dev.yml's backend service bind-mounts
# the repo root, so this lands on the host too, not just in the container)
tmp/
